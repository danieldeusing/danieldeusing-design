# dependencies (the package itself has none; this guards consuming experiments)
node_modules/

# OS / editor cruft
.DS_Store
*.log
.idea/
.vscode/*
!.vscode/extensions.json

# the build outputs in dist/ and tokens/tokens.json ARE committed on purpose
# (they are what jsDelivr serves and what native consumers read), so they are
# intentionally NOT ignored here.
